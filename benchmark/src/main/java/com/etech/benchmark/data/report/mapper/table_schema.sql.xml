<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.etech.benchmark.data.report.dao.TableSchemaDao">
    
    <insert id="addReportTableColumn" parameterType="java.util.List"  >
        insert into report_table_schema (
            id, table_fk, tableName, columnName, unit
        )
        values
        <foreach collection="list" item="item"  separator=","  >
            (#{item.id}, #{item.table_fk}, #{item.tableName}, #{item.columnName}, #{item.unit} )
        </foreach>
    </insert>
    
    <select id="checkTableSchemaIfExists" parameterType="map"  resultType="int">
        select count(1) from report_table_schema where  tableName = #{tableName}
    </select>
    
    <select id="checkTableIfExists" parameterType="map" resultType="int">
        SELECT count( 1 ) 
                FROM information_schema.TABLES
                WHERE table_name = #{tableName}
    </select>
    
</mapper> 